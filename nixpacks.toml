# Railway / Nixpacks build configuration.
# The viewer is a pure static site; install dependencies with pnpm and run `serve`.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["corepack enable", "pnpm install --prod --frozen-lockfile"]

[start]
cmd = "pnpm start"
